name: Deploy to Production
on:
  push:
    branches: [ main ]

jobs:
  deploy-production:
    runs-on: production
    steps:
    - name: Deploy to Production
      run: sudo -s /scripts/github-deploy-production.sh

